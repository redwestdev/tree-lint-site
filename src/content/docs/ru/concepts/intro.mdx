---
title: Вступление
description: 'Что умеет Tree-lint, из каких секций состоит конфигурация и как читать документацию.'
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';

Tree-lint — детерминированный линтер файловой структуры. Вы описываете в конфиге свои соглашения о файлах и папках, а Tree-lint проверяет их при каждом запуске — локально и в CI.

## Какие задачи решает

- **Борьба с хаосом в структуре** — соглашения о файлах и папках превращаются в исполняемые правила, которые проверяются при каждом запуске, а не на глаз при ревью.
- **Подготовка контракта для ИИ** — единый файл правил, который может прочитать ИИ-ассистент и которым проверяется результат его работы.
- **Ускорение онбординга** — новый разработчик видит принятую структуру и нарушения сразу, без пересказа «как у нас принято».

## Что умеет

- распознавать сущности по имени и составу папки;
- проверять имена, обязательный состав папки, длину имени, размер и число элементов;
- задавать собственные проверки через `custom` с любым сообщением;
- разделять блокирующие ошибки и предупреждения;
- ограничивать область проверки через `roots` и `ignore`;
- встраиваться в CI: при ошибках команда завершается с кодом `1`, без ошибок — с кодом `0`.

## Чего не обещает

- не разбирает содержимое кода и не проверяет импорты — только файловую структуру;
- не исправляет структуру автоматически — показывает нарушение, а исправляет его разработчик;
- не поддерживает символические ссылки — они пропускаются при сканировании.

> Tree-lint не привязан к конкретной архитектуре. Подойдёт слоистым и модульным структурам, легаси и проектам с генерацией кода — везде, где можно описать соглашение о файлах и папках.

## Конфигурация из пяти секций

Конфигурация состоит из пяти секций:

```ts
import { createConfig } from '@redwestdev/tree-lint';

export default createConfig({
  roots: ['src'], // директории для сканирования
  ignore: ['node_modules', 'dist'], // что пропускать
  entities: {}, // сущности
  layers: {}, // слои
  groups: {}, // группы (необязательно)
});
```

- `roots` и `ignore` — область сканирования: подробности на странице [Корни и исключения](/ru/configuration/roots-ignore/);
- `entities` — сущности: типы узлов, которые Tree-lint распознаёт и проверяет;
- `layers` — слои: папки, внутри которых ищутся сущности;
- `groups` — группы: правила для «обычных» папок по паттерну имени.

Поддерживаемые форматы файла конфигурации — на странице [Форматы конфигурации](/ru/configuration/config-formats/).

## Контракт для ИИ-ассистентов

Файл `tree-lint.config.ts` — это не только конфигурация линтера, но и исполняемый контракт о файловой структуре проекта:

- ассистент может прочитать те же правила, по которым пишете код вы: конфиг лежит в репозитории и описывает соглашения команды;
- результат работы ассистента проверяется тем же запуском `tree-lint scan`, что и ваш собственный код: отклонение от правил видно в выводе линтера.

## Маршруты чтения

Впервые с инструментом? Начните с [Быстрого старта](/ru/getting-started/quick-start/), чтобы получить первый результат, и возвращайтесь к справочным разделам по мере вопросов.

<CardGrid>
  <LinkCard title='Быстрый старт' href='/ru/getting-started/quick-start/' />
  <LinkCard title='Флоу работы' href='/ru/concepts/workflow/' />
  <LinkCard title='Сущности' href='/ru/concepts/entities/' />
  <LinkCard title='Слои' href='/ru/concepts/layers/' />
  <LinkCard title='Группы' href='/ru/concepts/groups/' />
  <LinkCard
    title='Форматы конфигурации'
    href='/ru/configuration/config-formats/'
  />
</CardGrid>
