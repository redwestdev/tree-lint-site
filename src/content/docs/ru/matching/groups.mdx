---
title: Определение группы
description: 'Как папки сопоставляются с группой: паттерн имени, обязательный состав children и границы применения.'
---

**Группа** — способ применить правила к папке по паттерну имени, не объявляя её сущностью. В отличие от `layers` и `entities`, `groups` — это один объект, а не словарь:

```ts
groups: {
  name: "[a-z]*",            // необязательный глоб для имени папки
  children: [                // если указано — обязательный состав папки
    { type: "file", name: "*.test.ts" },
  ],
  rules: {
    nameLength: { type: "warning", max: 20 },
  },
},
```

## Как работает сопоставление

- группа применяется только к «обычным» папкам — тем, что не стали слоями и не распознаны как сущности;
- внутри такой папки должен быть хотя бы один распознанный дочерний элемент: сущность или другая группа. Папка только с нераспознанными файлами под группу не попадёт;
- если задан `name` или `children` и совпадение неполное, выводится предупреждение распознавания — как у сущностей;
- `rules` применяются к самой папке.

Поле `name` — это micromatch-глоб имени папки, как у `matches.name` сущностей. Поле `children` работает так же, как `matches.children`: каждый перечисленный матчер должен найти себе отдельный дочерний элемент, лишние файлы и папки не мешают.

## Группа против сущности

Группа отвечает на тот же вопрос «что это за папка?», что и сущность, но без полного цикла «сущность → слой»:

- сущность нужно объявить в `entities` и привязать к слою (или к другой сущности), чтобы она распознавалась;
- группа сопоставляется сама по себе и годится для папок, которым не нужны слои, — например, каталоги тестов или сгенерированного кода.

## Дальше

- [Валидация группы](/ru/validation/groups/) — правила для папок группы;
- [Группы: понятие](/ru/concepts/groups/) — зачем нужны группы;
- [Определение сущности](/ru/matching/entities/) — как работает `matches` у сущностей.
